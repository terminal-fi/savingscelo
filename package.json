{
  "name": "savingscelo",
  "version": "1.0.0",
  "description": "SavingsCELO",
  "repository": {
    "url": "https://github.com/zviadm/savingscelo"
  },
  "dependencies": {},
  "devDependencies": {
    "@celo/contractkit": "^0.4.18",
    "@openzeppelin/contracts": "^3.2.0",
    "@typechain/truffle-v5": "^3.0.0",
    "bignumber.js": "^9.0.1",
    "celo-devchain": "^1.0.8",
    "truffle-typings": "^1.0.8",
    "typechain": "^4.0.0",
    "typescript": "^4.0.5"
  },
  "scripts": {
    "compile": "truffle compile && typechain --target=truffle-v5 'build/contracts/*.json'",
    "migrate": "npm run compile && npx tsc -p ./tsconfig.migrate.json && truffle migrate --reset",
    "test": "npx tsc -p ./tsconfig.test.json && truffle test --bail",
    "ganache": "npx celo-devchain --port 7545 --core v1"
  },
  "author": "Zviad Metreveli",
  "license": "ISC"
}
